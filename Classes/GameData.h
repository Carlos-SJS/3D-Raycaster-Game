#pragma once

const int SCREEN_HEIGHT = 567;
const int SCREEN_WIDTH = 1008;

const int UI_HEIGHT = 101;
const int UI_WIDTH = SCREEN_WIDTH;

const int PIXEL_SIZE = 1;

const float SCREEN_DIFF = (float)UI_HEIGHT / (float)PIXEL_SIZE;

const int GAME_HEIGHT = SCREEN_HEIGHT-UI_HEIGHT;